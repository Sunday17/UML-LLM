请分析以下需求文本，将其拆分为若干高内聚的模块。

【重要要求】
1. 每个模块的 core_requirements 必须详尽完整地描述该模块的所有需求，不得遗漏任何功能点。
2. 【关键】每个拆分出的模块，其包含的底层交互用例（Use Cases）数量严格限制在 1 到 15 个之间。
3. 每个用例条目应该是完整的句子，描述清楚"做什么"及"如何做"的关键细节。
4. 每个模块需要包含 module_name（模块名）、description（功能描述）、core_requirements（核心需求，字符串类型，必须详尽）。
5. 不要输出任何 JSON 之外的文字。
需求内容：
{text_content}