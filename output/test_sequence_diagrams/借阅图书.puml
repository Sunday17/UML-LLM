@startuml
title 自动生成：系统时序图
skinparam maxMessageSize 150
skinparam sequenceMessageAlign center

' 1. 声明参与者 (避免有些实体未主动发消息时不显示)

actor "师生"

actor "Book"

actor "BorrowRecord"

actor "User"


' 如果你之后从大模型提取了系统内部实体（如数据库、接口），也可以在这里声明为 participant
' 

' 2. 渲染交互消息流

    
"师生" -> "User" : 1. 查询借阅资格()
    
"师生" -> "Book" : 2. 检查可借状态()
    
"师生" -> "BorrowRecord" : 3. 创建借阅记录()
    
"BorrowRecord" -> "Book" : 4. 更新库存状态(减1)
    
"Book" -> "User" : 5. 更新用户借阅信息()
    


@enduml