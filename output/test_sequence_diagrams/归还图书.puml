@startuml
title 自动生成：系统时序图
skinparam maxMessageSize 150
skinparam sequenceMessageAlign center

' 1. 声明参与者 (避免有些实体未主动发消息时不显示)

actor "师生"

actor "Book"

actor "BorrowRecord"


' 如果你之后从大模型提取了系统内部实体（如数据库、接口），也可以在这里声明为 participant
' 

' 2. 渲染交互消息流

    
"师生" -> "Book" : 1. requestReturn()
    
"Book" -> "BorrowRecord" : 2. validateBorrowStatus()
    
"BorrowRecord" -> "Book" : 3. 确认可归还
    
"Book" -> "BorrowRecord" : 4. updateReturnStatus()
    
"BorrowRecord" -> "Book" : 5. 标记已归还
    
"Book" -> "师生" : 6. returnSuccess()
    


@enduml