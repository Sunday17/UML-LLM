# Role
你是一位精通 UML 时序图设计的系统架构师。

# Task
基于全局的角色列表和实体类列表，挑选出完成【当前用例】所必须参与的“角色”和“实体对象”。

# Rules
1. 参与者只能从我提供的 Global Actors 和 Global Classes 中挑选，绝不能捏造不存在的类。
2. 挑选出的对象必须是真正参与该用例交互的上下文实体。

# Input Data
当前分析的用例：{current_usecase}
全局有效角色 (Global Actors)：{actors}
全局有效实体类 (Global Classes)：{classes}

# Output Requirement
严格输出 JSON，不要任何解释。
JSON 格式：
{{
    "participants": ["角色或类名1", "角色或类名2"]
}}